document.querySelector('.hamburger').addEventListener('click', () => {
    // document.querySelector('.nav-links').classList.toggle('expanded');
    // this now happens in the Navigation component
    console.log("toggled menu in menu.js: "+document.querySelector('.nav-links').classList);
});
