---
import Hamburger from "./Hamburger.astro";
import Navigation from "./Navigation.astro";
import gsap from "gsap";

// import ThemeIcon from './ThemeIcon.astro';


---

<header>
    <a href="/" class="skip-link">
        <img class="mylogo" src="/jbj_logov2.svg" alt="JBJ logo" />
    </a>



    <nav class="centerstuff">
        <!-- <Hamburger transition:animate="slide" /> -->
        <Hamburger />
        <!-- <ThemeIcon /> -->
        <Navigation />
    </nav>

    <script is:inline>
// gsap.from(".mylogo", {strokeDasharray:0})
   gsap.set("svg", { opacity: 1 });
      // gsap.set("#rcirc", { scale: 1.3 });
      gsap.from("path", { strokeDashoffset: 38, duration: 0.6, ease: "none", stagger: 0.6 }, 0.08);
    </script>   

</header>
